spring:
  application:
    name: oauth-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: default
      label: master
      fail-fast: true

spring.config.import: configserver
